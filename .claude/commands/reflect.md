Consolidate the current session into episodic memory and surface inbound proposals.

Call the `gks_reflect` MCP tool with:
- `root`: `.`
- Include any session context from this conversation as the trace input.

After reflecting:
- Show how many episodic memories were written
- List any inbound proposals that were surfaced
- Suggest: "Review proposals with `gks inbound list --root=.`"

Use this at the end of a long working session to make sure insights are retained.
